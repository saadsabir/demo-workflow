{
  "name": "demo-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "express": "^4.17.1",
    "express-favicon": "^2.0.1",
    "mocha": "^6.2.2",
    "mocha-jenkins-reporter": "^0.4.2",
    "path": "^0.12.7",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "startnode": "node server.js",
    "start": "NODE_ENV=production react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "testjunit": "JUNIT_REPORT_PATH=./report.xml mocha --colors --reporter mocha-jenkins-reporter",
    "testxunit": "mocha test --reporter mocha-xunit-reporter"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "mocha-xunit-reporter": "^1.1.0"
  }
}
